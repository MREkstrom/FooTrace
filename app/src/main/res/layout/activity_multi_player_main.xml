<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_multi_player_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/secondaryBackground"
    tools:context="com.example.michael.footrace.MultiPlayerMain">


    <TextView
        android:text="Multi Player"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/mpMainTitle"/>

    <Button
        android:text="Join"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/mpMainTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="83dp"
        android:id="@+id/joinButton"
        android:onClick="onMPMainButtonClick"
        android:background="@drawable/ovalbuttonstyle"/>/>

    <Button
        android:text="Host"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/joinButton"
        android:layout_alignLeft="@+id/joinButton"
        android:layout_alignStart="@+id/joinButton"
        android:layout_marginTop="124dp"
        android:id="@+id/hostButton"
        android:onClick="onMPMainButtonClick"
        android:background="@drawable/ovalbuttonstyle"/>/>
</RelativeLayout>