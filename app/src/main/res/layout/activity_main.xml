<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/backgroundrevised"
    tools:context="com.example.michael.footrace.MainActivity">

    <Button
        android:text="Draw!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/drawButton"
        android:onClick="onMainMenuButtonClick"
        android:background="@drawable/beveledsquarebuttonstyle"
        android:layout_marginBottom="27dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Multi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/mpButton"
        android:onClick="onMainMenuButtonClick"
        android:background="@drawable/beveledsquarebuttonstyle"
        android:layout_above="@+id/drawButton"
        android:layout_alignLeft="@+id/drawButton"
        android:layout_alignStart="@+id/drawButton"
        android:layout_marginBottom="12dp" />

    <Button
        android:text="Single"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spButton"
        android:onClick="onMainMenuButtonClick"
        android:background="@drawable/beveledsquarebuttonstyle"
        android:layout_marginBottom="60dp"
        android:layout_alignBottom="@+id/mpButton"
        android:layout_alignLeft="@+id/mpButton"
        android:layout_alignStart="@+id/mpButton" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/settingsgear"
        android:id="@+id/settingsButton"
        android:background="@android:color/transparent"
        android:onClick="onMainMenuButtonClick"
        android:scaleX=".75"
        android:scaleY=".75"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
